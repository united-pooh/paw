# Activity docked sidebar visual evidence

- Changed files: `internal/ui/bubble/activity_layout.go`, `activity.go`, `layout.go`, `task_picker.go`, `app.go`, `header.go`, `status_line.go`, `task_card.go`
- Route/workflow: deterministic Bubble Tea `appModel.View()` fixtures generated by `TestCaptureActivityDockVisualFixtures`; Ctrl+G Activity workflow
- Terminal viewports: 120×30 cells and 80×24 cells
- Browser screenshot viewports: 1800×1000 px; narrow fixture 1400×900 px
- Artifacts:
  - `activity-docked-default.png`
  - `activity-docked-resized.png`
  - `activity-docked-preview.png`
  - `activity-docked-closed.png`
  - `activity-fullscreen-narrow.png`
  - `activity-docked-overlay-priority.png`
- Observed result:
  - Default and resized fixtures show one full-height `│` separator with `┬/┴` hairline joints; Activity participates in layout instead of covering workspace content.
  - Preview fixture keeps Activity visible while the left transcript switches to the task and the input remains part of the main workspace.
  - Closed fixture removes the floating task card and places `● N running · Ctrl+G` in the top hairline.
  - Narrow fixture renders Activity as the only inner page without transcript/input overlap.
  - Completion fixture keeps command candidates entirely inside the left workspace; the Activity pane remains visible and unchanged.
