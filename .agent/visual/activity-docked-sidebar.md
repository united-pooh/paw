# Activity docked sidebar visual evidence

- Changed files: `internal/ui/bubble/right_panel.go`, `command_helpers.go`, `activity_tasks_test.go`, `activity_visual_capture_test.go`, `bubble_test.go`; `internal/task/pool.go`, `pool_test.go`
- Route/workflow: deterministic Bubble Tea `appModel.View()` fixtures generated by `TestCaptureActivityDockVisualFixtures`; Ctrl+G Activity workflow
- Terminal viewports: 120×30 cells and 80×24 cells
- Browser screenshot viewports: 1800×1000 px; narrow fixture 1400×900 px
- Artifacts:
  - `activity-docked-default.png`
  - `activity-docked-resized.png`
  - `activity-docked-preview.png`
  - `activity-docked-closed.png`
  - `activity-fullscreen-narrow.png`
  - `activity-docked-overlay-priority.png`
- Observed result:
  - Default and resized fixtures show a single-line task header with worker role name on the left and explicit status/elapsed time on the right.
  - Worker names preserve their configured support colors (`八潮瑠唯 #669988`, `高松灯 #CC0033`, `千早爱音 #FF99CC`), including the selected row's contrasting background.
  - The selected running task expands a wrapped Chinese task description plus token metadata; unselected tasks remain one line, so the list uses vertical space according to actual content.
  - Attention sorting places the running worker first, the stopped worker before the completed worker, while selection remains attached to task ID.
  - Default/resized fixtures retain the full-height `│` separator and `┬/┴` joints; preview, narrow fullscreen, closed, and completion-priority layouts remain intact.
  - HTML fixtures now preserve ANSI true-color foreground/background styles before Playwright capture instead of stripping all colors.
