# Engineering Memory

## 2026-08-11 — 固定高度 TUI 的多行文本预算规则

### 适用规则

1. **单行包装器不得接收任意外部文本。** 名称或契约为 `*Line` 的函数只处理不含 `\n` 的单个物理行；Prompt、说明、错误、用户输入等可能包含显式换行的数据必须先按逻辑行拆分，或直接使用 `*Text` 多行包装器。
2. **高度预算必须以最终物理行数计算。** 固定高度 panel 不能用“slice 元素数”代替终端实际行数；任何 slice 元素内部仍含 `\n`，都会使预算、滚动范围和最终裁剪失真。
3. **裁剪前先规范化，再做布局。** 正确顺序是：净化控制字符 → 保留/拆分显式换行 → 按 terminal cell 软换行 → 得到物理行 → 分配高度 → 最终裁剪。不要先按错误的逻辑行数分配预算，再依赖 `fitStyledRect` 补救。
4. **固定结构区必须有语义可见性回归测试。** 测试不能只验证输出矩形尺寸，也不能只断言标题存在；还要使用真实形态的长、多行内容，断言业务选项、状态、确认/取消操作和帮助行仍可见或可滚动到。
5. **边界测试必须覆盖显式换行与软换行的组合。** 至少包含空行、连续换行、CJK、长单行、长描述，以及“多行 Prompt + 固定底部操作”的场景。
6. **重构后的 Git blame 不等于缺陷首次引入点。** helper 替换、宽度系统迁移等重构可能接管一条已有错误语义。定位回归时应同时查看文件首次引入提交、`git log -L` 和重构前实现，区分：根缺陷引入、缺陷传播、触发概率上升。
7. **扩大组件使用范围前，先扩大输入域测试。** 当系统提示开始强制模型更多使用某个交互组件时，该组件会从短演示文本进入真实长文本场景；功能推广提交应补充生产形态输入测试，而不能只依赖原有 happy-path fixture。

### 本次实例

Select dock 曾把可能含多行设计说明的 Prompt 交给单行包装器。逻辑上只预留最多 3 行，但包装结果内部保留了真实换行，固定高度裁剪因此只显示标题和 Prompt，答案及底部操作全部被截掉。

根缺陷最早随 `b5759192`（`feat: add interactive Select dock workflow`）引入；`0c5934eb` 的 terminal-cell 宽度重构把旧单行 helper 替换成新的 `wrapStyledCellLine`，保留了同一错误而不是首次制造它；`c753debe` 推广 Select 用于结构化问题后，提高了长 Prompt 触发该问题的概率。

修复原则：Prompt 的高度估算与实际渲染统一使用多行感知的 `wrapStyledCellText`，并用带显式空行的 Select 请求断言答案和固定操作仍然可见。
