# Diff 渲染演示

## 项目简介

这是一个用于验证 Coding Agent 文件编辑能力的示例文档。
它包含多个结构化段落，可以直观观察新增、删除和替换产生的差异。
此外，本次修改会保留标题结构，只调整段落内容。

## 功能说明

程序可以安全读取和修改工作区内的本地文件，并将执行结果显示在终端中。
当前版本除了基础文件操作，还支持内容搜索、命令执行以及错误信息反馈。

## 使用方法

启动 Agent 后，用自然语言描述需要创建或修改的文件。
Agent 会执行对应操作，并可以通过统一 diff 展示修改前后的内容。
建议在应用修改后运行测试或检查 Git 状态，确认结果符合预期。

## 后续计划

后续计划增加更精细的补丁编辑、彩色 diff 渲染和修改确认机制。
同时会继续改善多文件变更的摘要与回滚体验。
